---
# ============================================================
# inventories/production/hosts.yml
# Inventario de PRODUCCIÓN - Dispositivos reales
# ============================================================
# ADVERTENCIA: Solo usar con dispositivos físicos reales.
# Siempre hacer backup antes de ejecutar en producción.
# ============================================================

all:
  children:

    cisco_ios:
      hosts:
        # Agregar dispositivos reales aquí
        # PROD-R1:
        #   ansible_host: <IP_REAL>
        #   device_role: core_router

    cisco_iosxe:
      hosts: {}

    firewalls:
      hosts: {}

    all_routers:
      children:
        cisco_ios:
        cisco_iosxe:

    network_devices:
      children:
        all_routers:
        firewalls:
